appId: 'fr.deltaarts.skyblocklauncher'
productName: 'SkyBlock Launcher'
artifactName: '${productName}-setup-${version}.${ext}'

copyright: 'Copyright © 2026 Delta Arts'

asar: true
compression: 'maximum'

files:
  - '!{dist,.gitignore,.vscode,docs,dev-app-update.yml,.nvmrc,eslint.config.mjs}'

extraResources:
  - 'libraries'

# Windows Configuration
win: 
  target: 
    - target: 'nsis'
      arch: 'x64'

# Windows Installer Configuration
nsis:
  oneClick: false
  perMachine: false
  allowElevation: true
  allowToChangeInstallationDirectory: true

# macOS Configuration
mac:
  target:
    - target: 'dmg'
      arch:
        - 'x64'
        - 'arm64'
  artifactName: '${productName}-setup-${version}-${arch}.${ext}'
  category: 'public.app-category.games'

# Linux Configuration
linux: 
  target: 'AppImage'
  maintainer: 'Delta Arts'
  vendor: 'Delta Arts'
  synopsis: 'SkyBlock Launcher - Hypixel SkyBlock Client'
  description: 'Launcher Minecraft Fabric premium pour Hypixel SkyBlock avec mods performance, QoL et visuels. Par Delta Arts.'
  category: 'Game'


directories:
  buildResources: 'build'
  output: 'dist'